package com.app.arejaybee.character_sheet;

public class Utility {
    public static int roundDown(double num) {
        if (num < 0) {
            return (int) Math.floor(num);
        }
        return (int) Math.floor(num);
    }
}
